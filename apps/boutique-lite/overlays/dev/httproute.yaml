apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: boutique-lite
spec:
  hostnames:
    - app.example.com
  parentRefs:
    - name: public-gateway
      namespace: default
  rules:
    - backendRefs:
        - name: boutique-lite
          port: 80
